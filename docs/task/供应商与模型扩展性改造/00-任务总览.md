# 供应商与模型扩展性改造 · 任务总览

## 任务目标

「说吧！」目前所有识别能力都绑定在阿里云百炼（`funasr`）单一供应商上：后端在多处硬编码供应商 ID 与 `kind` 检查，模型能力信息（协议族、热词支持、时间戳支持、可用场景）以字符串前缀判断的形式散落在前后端十余处。本次改造的目标是：**建立"模型注册表 + 供应商连接器"两层机制，使得未来新增一个同族新模型只需改 1 个数据文件，新增一个新供应商只需新增 1 个 provider 模块 + 注册表登记**，并为后续引入 LLM 等新能力类型（如识别结果后处理）预留能力机制。

**属于本次任务**：模型注册表（单一数据源）及前后端散落判断的收敛；实时识别链路的供应商连接器 trait 抽象；解除后端单供应商硬锁（`normalize_settings` / 命令层硬编码）；供应商默认值结构的多能力化（预留 `llm`）；前端供应商层泛化；用一个真实新模型验证流程并沉淀《新增供应商与模型操作手册》。

**不属于本次任务**：实现任何 LLM 后处理功能（仅预留机制）；接入任何新的第三方供应商；重写文件转写（OSS 上传 / 同步短音频 / SSE）的协议抽象（阿里云内部按模型分支保留，仅把判断依据挪入注册表）；UI 视觉改动；热词功能行为变更。

## 当前进度

- 当前阶段：第四阶段-流程固化与验证（已完成）
- 当前任务：无，全部 6 个任务已完成
- 整体状态：4.1 用户实测已通过（新模型听写注入、实时字幕均正常），验证用模型已按约定移除；2.1、3.1、3.2 涉及的历史行为（听写注入/字幕/静音测试/录音识别/热词/设置页外观）用户实测仍待统一确认
- 已完成任务数量：6
- 总任务数量：6
- 当前阻塞问题：无

## 阶段列表

| 阶段 | 阶段目标 | 状态 |
|---|---|---|
| 第一阶段-模型注册表 | 建立模型元数据单一数据源，前后端所有按模型名的散落判断改为从注册表派生 | 已完成 |
| 第二阶段-实时连接器抽象 | 把实时识别的协议族差异收敛为 trait，会话循环与供应商解耦 | 已完成 |
| 第三阶段-多供应商基础设施 | 解除后端单供应商硬锁，默认值结构多能力化，前端供应商层泛化 | 已完成 |
| 第四阶段-流程固化与验证 | 用一个真实新模型走通"按流程新增"，沉淀操作手册 | 已完成 |

## 任务清单

| 编号 | 任务名称 | 状态 | 前置任务 | 任务文件 |
|---|---|---|---|---|
| 1.1 | 建立模型注册表与后端接入 | 已完成 | 无 | [1.1-建立模型注册表与后端接入.md](任务/第一阶段-模型注册表/1.1-建立模型注册表与后端接入.md) |
| 1.2 | 前端模型选项与能力判断改为注册表派生 | 已完成 | 1.1 | [1.2-前端模型选项与能力判断改为注册表派生.md](任务/第一阶段-模型注册表/1.2-前端模型选项与能力判断改为注册表派生.md) |
| 2.1 | 实时识别连接器trait化 | 已完成 | 1.1 | [2.1-实时识别连接器trait化.md](任务/第二阶段-实时连接器抽象/2.1-实时识别连接器trait化.md) |
| 3.1 | 解除后端单供应商锁与默认值多能力化 | 已完成 | 2.1 | [3.1-解除后端单供应商锁与默认值多能力化.md](任务/第三阶段-多供应商基础设施/3.1-解除后端单供应商锁与默认值多能力化.md) |
| 3.2 | 前端供应商层泛化与硬编码清理 | 已完成 | 3.1 | [3.2-前端供应商层泛化与硬编码清理.md](任务/第三阶段-多供应商基础设施/3.2-前端供应商层泛化与硬编码清理.md) |
| 4.1 | 以新模型走通新增流程并沉淀操作手册 | 已完成 | 1.2、2.1、3.1、3.2 | [4.1-以新模型走通新增流程并沉淀操作手册.md](任务/第四阶段-流程固化与验证/4.1-以新模型走通新增流程并沉淀操作手册.md) |

任务状态取值：待开始 / 进行中 / 已阻塞 / 待验证 / 已完成 / 已取消。

## 重要说明

- **持久化兼容是硬约束**：用户本地已有 `say-it-state.json`（含 `providers.defaults.asr`、funasr profile 的 apiKey / 热词 / 词表 ID）。任何对 `ProviderSettings` / `ProviderDefaults` 结构的改动都必须能无损反序列化旧 JSON（`#[serde(default)]` 兜底），不得导致用户丢 Key 或丢热词。
- **行为不变原则**：第一、二、三阶段是纯重构，听写 / 实时字幕 / 录音识别 / 热词的对外行为不允许变化；每阶段完成后用相同操作路径回归。
- 涉及麦克风、快捷键、注入等真实交互的验证，改完后列出操作步骤交给用户实测，不要自行模拟。
- 执行任何任务前，先阅读本文件、[01-实施方案.md](01-实施方案.md) 和对应任务文件。

## 最近记录

### 2026-07-06

- 建立任务计划：分 4 个阶段、6 个任务。前置分析结论：听写停止时机（finish-task → task-finished）实现正确无需改动；扩展性瓶颈在单供应商硬锁与模型判断散落，已在实施方案中列出全部硬编码位置清单。
- **完成任务 1.1**：创建 `shared/asr-models.json` 模型注册表，新增 `src-tauri/src/providers/registry.rs` 模块，后端所有散落的模型判断逻辑改为查询注册表，添加 11 个单元测试全部通过。采用方案 A（共享 JSON 文件）。
- **完成任务 1.2**：新增 `ui/src/features/asr/modelRegistry.ts`，重写 `modelOptions.ts` 从注册表派生，前端所有模型选项与能力判断不再有硬编码，类型检查和构建验证通过。
- **第一阶段已完成**：模型注册表单一数据源已建立，前后端散落判断已收敛为查表，行为与改造前完全一致。
- **完成任务 2.1**：新增 `providers/connector.rs`（`AsrEvent` + `RealtimeAsrConnector` trait）与 `providers/alibabacloud/realtime.rs`（`DashscopeDuplexConnector` / `QwenRealtimeConnector` 两个实现 + 工厂函数），`commands/asr.rs` 的会话循环与静音测试改用连接器，5 处 `match family` 全部消除，静音测试的"仅支持 duplex"判断也收敛为 `registry::realtime_family_supports_silence_test`，`commands/asr.rs` 不再出现 `RealtimeAsrFamily`。新增 8 个连接器单测，`cargo test` 49 个全部通过，`cargo check` 无警告。用户实测（Fun-ASR/Qwen 听写注入、字幕、静音测试）待确认。
- **第二阶段已完成**：实时识别协议差异已收敛为 trait，新增协议族只需新增一个连接器实现 + 工厂分支，会话循环零改动。
- **完成任务 3.1**：`providers/mod.rs` 的 `normalize_settings` 不再删除未知 profile，改为"内置 profile 缺失则插入、存在则只修正固定字段"；`ProviderDefaults` 增加 `llm` 字段（旧 JSON 无损兼容）；`has_capability`/`default_provider_id`/`set_default_provider` 不再硬编码 `"asr"`；新增 `realtime_connector_for` 工厂按 `kind` 分发（当前仅 `alibabacloud-funasr` 一个分支），`commands/asr.rs` 不再直接依赖 `FunAsrParams`；`commands/transcription.rs` 的取配置函数重命名并改走 `resolve_provider_id` + kind 校验。新增 4 个单测，`cargo test` 53 个全部通过。
- **完成任务 3.2**：前端 `ProviderCapability` 扩展为 `"asr" | "llm"`，`effective()` 兜底不再硬编码 `"funasr"`，改为"defaults 查不到则取第一个具备该能力的 enabled profile"；`ensureDictationProviderReady`/`providerHasApiKey`/`TranscriptionView`/`TranscriptAlignPanel` 的供应商判断全部改为按生效供应商派生；`SettingsProviderPanel` 改按 profile 列表取值（视觉不变），热词面板仅在 funasr profile 存在时渲染。`npx tsc --noEmit`、`npm run ui:build` 均通过。
- **第三阶段已完成**：后端不再假设单供应商，`ProviderDefaults` 可表达多能力；前端供应商层不再硬编码 `"funasr"`。2.1、3.1、3.2 的用户实测（听写注入/字幕/静音测试/录音识别/热词/设置页外观）待用户统一确认。
- **完成任务 4.1**：与用户确认验证模型为 `paraformer-realtime-v2`（验证后移除）。按场景一流程只改了 `shared/asr-models.json`（新增一条记录）和 `registry.rs` 的 `test_model_count` 断言值（9→10，纯数据变化引起的测试快照更新），未触碰任何协议/连接器/命令层/前端代码，证明"同协议族新模型只改注册表"在当前架构下成立。用户实测确认新模型出现在预期下拉，听写注入与实时字幕均正常。验证通过后已移除该条目、`test_model_count` 改回 9，`cargo test` 53 个全部通过。已编写《新增供应商与模型操作手册》至 `docs/rules/新增供应商与模型操作手册.md`，覆盖同族新模型/新协议族/新供应商/新能力类型四个场景。
- **第四阶段已完成，全部 6 个任务已完成**：模型注册表、实时连接器 trait、多供应商基础设施均已建立并通过真实新模型验证；同族新模型的新增成本已收敛为"改 1 个数据文件"。2.1、3.1、3.2 涉及的历史行为用户实测仍待统一确认。
