# 录音识别与文稿对齐 · 任务总览

## 任务目标

在「说吧！」左侧导航新增「录音识别」页面，接入阿里云百炼 Fun-ASR **录音文件识别**（异步 HTTP API），复用现有 Fun-ASR 实时识别的同一个 API Key。音频文件通过阿里云百炼**临时 OSS**（getPolicy + 表单直传）上传获得 `oss://` 临时 URL。识别结果支持两种展示：纯文本、带时间戳字幕（可导出 SRT）。在此基础上提供**文稿对齐**功能：用户提供音频 + 一行一句的文稿，用识别结果的词级时间戳与文稿做对齐，生成文本 100% 遵循用户文稿、时间轴精确的字幕。

**属于本次任务**：临时 OSS 上传、录音文件识别任务提交/轮询/结果解析（Rust 后端）、录音识别页面（文件选择、可折叠参数、结果展示、SRT 导出）、文稿对齐算法与界面。

**不属于本次任务**：实时识别/语音输入/实时字幕既有功能的改动、正式 OSS 存储、批量文件队列、字幕编辑器、说话人分离结果的可视化优化（仅透传参数）。

## 当前进度

- 当前阶段：第三阶段
- 当前任务：3.2 已实现，下一个任务 3.3（端到端验证）
- 整体状态：全部功能实现完毕，等待用户真实音频端到端实测
- 已完成任务数量：1
- 总任务数量：8
- 当前阻塞问题：真实音频端到端联调、文件选择/拖放/导出交互实测待执行

## 阶段列表

| 阶段 | 阶段目标 | 状态 |
|---|---|---|
| 第一阶段-后端识别能力 | Rust 侧打通：临时 OSS 上传 → 提交识别任务 → 轮询 → 解析结构化结果 | 进行中 |
| 第二阶段-录音识别页面 | 新增导航与页面，完成文件选择、参数折叠面板、识别流程、双视图结果展示与导出 | 进行中 |
| 第三阶段-文稿对齐 | 实现文稿与词级时间戳的对齐算法及对应界面、SRT 导出 | 进行中 |

## 任务清单

| 编号 | 任务名称 | 状态 | 前置任务 | 任务文件 |
|---|---|---|---|---|
| 1.1 | 临时OSS上传模块 | 已完成 | 无 | [1.1-临时OSS上传模块.md](任务/第一阶段-后端识别能力/1.1-临时OSS上传模块.md) |
| 1.2 | 录音文件识别任务与结果解析 | 待验证 | 1.1 | [1.2-录音文件识别任务与结果解析.md](任务/第一阶段-后端识别能力/1.2-录音文件识别任务与结果解析.md) |
| 2.1 | 页面框架与文件选择 | 待验证 | 无（可与第一阶段并行） | [2.1-页面框架与文件选择.md](任务/第二阶段-录音识别页面/2.1-页面框架与文件选择.md) |
| 2.2 | 识别流程与参数面板 | 待验证 | 1.2、2.1 | [2.2-识别流程与参数面板.md](任务/第二阶段-录音识别页面/2.2-识别流程与参数面板.md) |
| 2.3 | 结果展示与导出 | 待验证 | 2.2 | [2.3-结果展示与导出.md](任务/第二阶段-录音识别页面/2.3-结果展示与导出.md) |
| 3.1 | 文稿对齐算法 | 待验证 | 1.2 | [3.1-文稿对齐算法.md](任务/第三阶段-文稿对齐/3.1-文稿对齐算法.md) |
| 3.2 | 文稿对齐界面与导出 | 待验证 | 2.2、3.1 | [3.2-文稿对齐界面与导出.md](任务/第三阶段-文稿对齐/3.2-文稿对齐界面与导出.md) |
| 3.3 | 端到端验证与经验沉淀 | 待开始 | 2.3、3.2 | [3.3-端到端验证与经验沉淀.md](任务/第三阶段-文稿对齐/3.3-端到端验证与经验沉淀.md) |

任务状态取值：待开始 / 进行中 / 已阻塞 / 待验证 / 已完成 / 已取消。

## 重要说明

- 复用 `funasr` 供应商配置里的同一个 `apiKey`（阿里云百炼），不新增密钥。
- 临时 OSS 的 `oss://` URL 有效期 48 小时、上传凭证接口限流 100 QPS，官方注明**不建议生产环境**；本应用为个人桌面工具、单文件低频调用，接受该限制（见 重要记录 002）。
- 现有热词词表以 `target_model=fun-asr-realtime` 创建，对录音文件识别模型 `fun-asr` **可能不生效**，暂不自动复用（见 重要记录 003，待确认）。
- 文件选择需要新增 `tauri-plugin-dialog` 依赖（见 重要记录 004）。
- 执行任何任务前，先阅读本文件、[01-实施方案.md](01-实施方案.md) 和对应任务文件。

## 最近记录

### 2026-07-03

- 建立任务计划：分 3 个阶段、8 个任务；确认 API 细节（异步转写、临时 OSS 上传流程、结果 JSON 结构）并写入实施方案。
- 完成 1.1 临时 OSS 上传模块：新增 Rust 协议实现与 reqwest multipart 依赖；真实上传验证延后到 1.2 联调。
- 完成 1.2 后端实现：新增录音文件识别提交、轮询、结果解析协议与 Tauri 后台任务命令；`cargo check`、`npx tsc --noEmit` 已通过，真实音频端到端验证待执行。
- 完成 2.1 页面框架实现：新增「录音识别」导航与页面 Tab、文件对话框依赖、拖放监听和本地文件信息读取命令；`cargo check`、`npx tsc --noEmit`、`npm run ui:build` 已通过，文件选择/拖放待用户实测。
- 完成 2.2/2.3 前端功能实现：接入识别开始/取消、事件进度、参数折叠面板与持久化、纯文本/字幕双视图、SRT 导出和写文件命令；`cargo check`、`npx tsc --noEmit`、`npm run ui:build` 已通过，真实音频识别与导出待用户实测。
- 完成 3.1 文稿对齐算法：`text_align.rs` 实现 token 规整（含中文数字等价）、唯一 n-gram 锚点分治 + 半全局 Needleman-Wunsch + 带宽兜底、行时间内插与单调后处理，13 个单元测试全绿；新增 `align_transcript` 命令；真实识别数据验证与 3.2 联调时一并执行。设计细节见 3.1 任务文件“设计定稿”。
- 完成 3.2 文稿对齐界面：新增 `TranscriptAlignPanel` 与文件选择共享模块（拖放按当前 Tab 路由）、对齐侧独立状态与识别结果缓存、低匹配行黄色标注与平均匹配率、SRT 导出（`音频名.对齐.srt`）；`npx tsc --noEmit`、`npm run ui:build` 通过，真实音频实测转交用户（见 3.2 执行步骤 4）。
- 增强：对齐结果支持「完全按文稿」/「识别修正」双视图切换——相似度（双向 Dice）< 0.4 的差异行在修正版中改用识别文本，两版可分别导出；底层对齐改为仿射 gap 计分（Gotoh），修复长即兴插入导致的行尾时间错误。详见 3.1 设计定稿 7/8 条与 3.2 执行记录。
