# 11.1 Fun-ASR 空终态修复交接

## 结果

- Say-It 已精确锁定 `@henjicc/ai-sdk@0.2.3`，依赖锁指向 GitHub Packages 的正式包。
- 构建脚本、Rust bundle 断言和内置在线运行时临时目录均同步到 `0.2.3`。
- QuickJS capabilities bundle 已从 0.2.3 重建；没有修改 WebSocket 桥、听写状态机或模型选择逻辑。

## 修复语义

SDK 0.2.3 会把百炼官方 Fun-ASR 空句始帧：

```json
{"sentence_begin":true,"sentence_end":false,"text":""}
```

作为合法生命周期事件静默推进。空 final、畸形空 partial、全程没有有效 final 仍严格失败。空 `task-finished.payload` 只结束会话，不覆盖已得到的最终文本。

## 版本与产物

- Package ID：`1181242908`
- shasum：`fe0fec961b65b6e67648f97df8c0f7cc5b07aacc`
- integrity：`sha512-l/s48EeQvOyGU5w0+xF3EKIRZmHwlk37qVX2ftMGBJUx8xQNNDvq7FSUAGpDaGYzVCmR5gquSvb4atyYuVZOPg==`
- capabilities bundle：33 个模块，56719 bytes，sha256 `84041cff53e6aea67a7099141e645c92b46210c338b24b82ca84969c37bae62d`

## 验证

- `npm run sdk-runtime:typecheck`：通过。
- `npm run sdk-runtime:build`：通过，manifest 确认 0.2.3 与正式 integrity/shasum。
- 新增宿主桥集成回归：本地脚本 WebSocket 依次发送 `task-started`、官方空句始、有效 final、空 payload 的 `task-finished`，最终文本与事件序列正确，HTTP/WS/流资源均归零。
- `cargo test --manifest-path src-tauri/Cargo.toml providers::sdk_runtime -- --nocapture`：17 通过、0 失败、1 个真实付费用例按设计忽略。
- 未发起真实付费请求。

## 用户真实验收

Tauri 重启后可直接用 `fun-asr-realtime` 或日期版重复麦克风识别。重点确认不再出现 `Bailian Fun-ASR result has no text`，同时能正常得到 final。若仍失败，应保存新的脱敏错误与事件顺序；不要记录 API Key 或原始音频。
