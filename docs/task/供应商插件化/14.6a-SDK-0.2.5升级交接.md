# 14.6a：Say-It 升级 SDK 0.2.5 交接

更新时间：2026-08-28

## 完成边界

- `@henjicc/ai-sdk` 已从 `0.2.4` 精确锁定到 `0.2.5`。
- lock 指向 GitHub Packages 已发布包：
  - version id：`1182474800`
  - shasum：`b3b80f250d943ac2b23ac4c95b5837dacc2d93dd`
  - integrity：`sha512-6WCgQEWM+EenbBT7njFQEx21jlwrO9KVIdJHHeXula8vImZ3fgcLjnEs+B1W6HaYqv2Cd5DCMk+Ug0wO0eNrLA==`
- QuickJS 仍只构建三组 Say-It 已选择能力：百炼 ASR/实时 ASR/翻译、Groq、外部插件 LLM module。
- 没有新增 BigModel 产品入口、供应商设置界面、凭据或模型配置。

## 按需隔离与兼容结论

构建脚本继续禁止 capability、Groq 和外部 LLM module bundle 引入 generation；Groq 与外部 LLM module 还继续禁止引入 `dist/llm/bigmodel/`。Groq 使用原有的 endpoint identity 构建替换，只接受 Groq 且不注册 endpoint profile。

0.2.5 新增的 preset/custom setup、builtin/user lifecycle 和 `apiKeyUrl` 安全契约均为可选公共字段。Say-It 当前不消费供应商设置契约，因此没有新增宿主分支。升级后 bundle 模块数保持为 capabilities 33、Groq 100、外部 LLM modules 12；产物未出现 GLM 模型、BigModel 区域地址或 `apiKeyManagementUrl`，现有 ASR、翻译、Groq、插件 LLM、取消与资源释放行为保持通过。

## 修改位置

- `package.json`、`package-lock.json`：SDK 版本、远端包地址与完整性锁定。
- `scripts/build-ai-sdk-runtime.mjs`：0.2.5 精确版本和 GitHub Packages 地址断言。
- `src-tauri/src/providers/sdk_runtime/bundle_tests.rs`：0.2.5 版本、integrity 和运行时断言。
- `src-tauri/src/providers/sdk_runtime/online.rs`：内置运行时临时目录按 0.2.5 隔离。

## 验证结果

- `npm run sdk-runtime:typecheck`：通过。
- `npm run sdk-runtime:build`：通过；capabilities 33 modules、Groq 100 modules、外部 LLM modules 12 modules。
- `cargo test --manifest-path src-tauri/Cargo.toml providers::sdk_runtime::bundle_tests -- --nocapture`：6/6 通过。
- `cargo test --manifest-path src-tauri/Cargo.toml providers::sdk_runtime::tests -- --nocapture`：7/7 通过。
- `npm run typecheck`：通过。
- `npm run test:ui`：10 files / 28 tests 通过。
- `npm run ui:build`：通过。
- `npm ls @henjicc/ai-sdk --depth=0`：仅解析到 `0.2.5`。

以上均为 fixture、本地脚本或受控本地传输测试，没有发起真实供应商或付费请求。

## 执行说明与后续边界

本机仓库 `.npmrc` 和 `gh` 登录令牌访问 GitHub Packages npm 端点时返回 401；14.5 已完成远端回装并将相同 tarball 放入 npm 内容缓存，因此本次依据发布交接中的远端 resolved、shasum 与 integrity 使用离线缓存完成精确安装。最终 `node_modules`、manifest 与 lock 三者均为 0.2.5，未改变发布包内容或校验值。

若 Say-It 后续选择接入供应商设置公共契约或 BigModel，必须单独建立产品入口和区域凭据边界；不能因本次依赖升级默认加载相关 module。工作区同时存在其他任务的 Rust、UI 与生成 schema 未提交改动，本任务没有格式化、暂存或提交这些文件。
