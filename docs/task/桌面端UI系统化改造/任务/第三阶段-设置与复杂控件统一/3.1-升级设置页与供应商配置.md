# 3.1 升级设置页与供应商配置

## 任务状态

- 状态：已完成
- 所属阶段：第三阶段-设置与复杂控件统一
- 前置任务：1.2
- 关联任务：3.2、4.1、4.2
- 是否可以并行：可以。可与 2.1、2.2、3.2 在 1.2 完成后并行，但需避免同时修改基础组件。

## 任务目标

升级设置页主结构、密钥与识别、启动设置、麦克风与提示音、外观页面，使设置区域与核心页面使用一致的桌面端视觉体系，并保留现有自动保存、折叠、高级参数和本地偏好逻辑。

## 当前情况

- `ui/src/views/SettingsView.tsx` 使用 tabs 切换密钥与识别、录音调整、启动设置、麦克风与提示音、外观。
- `ui/src/views/SettingsProviderPanel.tsx` 管理 Fun-ASR API Key、热词、高级参数和供应商状态。
- `ui/src/views/SettingsStartupPanel.tsx` 管理开机自启与静默启动。
- `ui/src/views/SettingsMicCuePanel.tsx` 管理输入设备、麦克风保活、提示音和自定义音频文件。
- `ui/src/views/SettingsAppearancePanel.tsx` 管理暗色/亮色和强调色。
- `ui/src/components/ui/Collapse.tsx` 已用于供应商配置和嵌套设置。
- 当前设置页视觉相对接近旧黑底卡片，tabs 与参考图一致性不足，折叠面板、输入框、按钮、说明文字仍需统一。

## 实施方案

保留设置页 tabs 和各子面板数据逻辑，统一设置页容器、tabs、折叠面板、API Key 输入区、高级参数表单、启动设置和外观预览。设置页不新增未确认入口，不改变 API Key 自动保存行为。

## 执行步骤

1. 调整 `SettingsView.tsx`：补充统一页头或设置页专用容器，保持 tabs key 不变。
2. 调整 `SettingsProviderPanel.tsx`：供应商折叠面板、API Key 输入、状态文案、热词和高级参数折叠区使用统一 token 和控件规格。
3. 保留 API Key 自动保存、防抖、显示/隐藏、远程读取真实 key、打开获取页面等逻辑不变。
4. 调整 `SettingsStartupPanel.tsx`：使用统一 section 和 checkbox/switch 布局，保留 Tauri 启动设置命令。
5. 调整 `SettingsMicCuePanel.tsx`：输入设备、保活秒数、提示音选择、自定义文件选择和试听按钮布局统一。
6. 调整 `SettingsAppearancePanel.tsx`：暗/亮色切换、强调色编辑、预览区、恢复默认按钮与 token 体系一致。
7. 记录人工验证步骤：API Key 输入/显示、折叠展开、启动设置开关、提示音文件选择、外观切换。
8. 运行 `npx tsc --noEmit` 和 `npm run ui:build`。
9. 更新本任务执行记录和 `00-任务总览.md`。

## 涉及内容

### 新增文件

- 默认不新增；必要时新增设置页局部复用组件到 `ui/src/components/ui/`。

### 修改文件

- `ui/src/views/SettingsView.tsx`：设置页容器和 tabs 布局。
- `ui/src/views/SettingsProviderPanel.tsx`：供应商配置面板视觉统一。
- `ui/src/views/SettingsStartupPanel.tsx`：启动设置布局。
- `ui/src/views/SettingsMicCuePanel.tsx`：麦克风与提示音布局。
- `ui/src/views/SettingsAppearancePanel.tsx`：外观设置布局。
- `ui/src/components/ui/Collapse.tsx`：如折叠组件规格需要统一则修改。

### 可能废弃

- 设置子面板内重复的卡片、折叠、提示文案样式：迁移到统一组件后删除。

## 验收标准

- [x] 设置页 tabs、折叠面板和表单控件与主 UI 风格一致。
- [x] API Key 自动保存、显示/隐藏、打开获取链接功能保留。
- [x] 开机自启、静默启动、提示音、自定义音频文件选择、外观主题切换功能保留。
- [x] `npx tsc --noEmit` 和 `npm run ui:build` 通过，或记录无法执行原因。
- [x] 人工交互验证步骤已记录在执行记录中。
- [x] 任务总览已经同步更新。

## 执行记录

### 2026-07-04

- 完成内容：设置页新增统一 `PageHeader`；供应商配置、启动设置、麦克风与提示音、外观四个子面板全部迁移到令牌体系与统一布局组件。
- 实际改动：
  - `SettingsView.tsx`：加 `PageHeader（设置）`，容器改 `flex flex-col gap-7`，tabs key 与面板切换逻辑不变。
  - `SettingsProviderPanel.tsx`：外层包 `SettingsSection（识别供应商）`；获取链接文案、API Key 输入、显隐眼睛按钮、状态文案、语种提示、嵌套折叠底色（`bg-black/20`→`bg-[var(--color-bg)]`）全部令牌化；保留 API Key 自动保存、防抖、显隐、远程读取真实 key、打开获取页面逻辑。
  - `SettingsStartupPanel.tsx`：`Card`→`SettingsSection`，新增本文件内 `ToggleRow`（标题+说明+`Switch`）承载开机自启/静默启动，保留 Tauri 启动命令与联动禁用。
  - `SettingsMicCuePanel.tsx`：`Card`→`SettingsSection`+`FormGrid`；「启用音频提示」改为开关行；文件选择/试听按钮与说明文字令牌化；保留提示音选择与自定义文件读取逻辑。
  - `SettingsAppearancePanel.tsx`：`Card`→`SettingsSection`；色调切换、强调色卡片、预览区全部令牌化（`#ff4d4f/#ff8589`→`var(--color-err)`，`rounded-xl/2xl`→令牌半径）；保留取色器与 Hex 校验（用户自定义颜色为允许的硬编码例外）。
- 验证结果：`npx tsc --noEmit` 通过；`npm run ui:build` 通过（106 模块）。
- 待用户人工验证（涉及鼠标/输入交互，代码侧不自测）：
  1. 设置页五个 tab 切换正常，页头「设置」固定显示。
  2. 密钥与识别：输入 API Key 约 0.5s 后自动保存并提示；点眼睛可显示/隐藏（已配置时首次点击会远程读取真实 key）；「点击此处获取 API Key」能打开外部页面；展开「热词」「高级参数」折叠，保存高级参数有提示。
  3. 启动设置：切换「开机自启」开关；关闭自启时「静默启动」置灰不可点，开启后可切换。
  4. 麦克风与提示音：切换「启用音频提示」开关；选择开始/结束提示音；「选择开始/结束音文件」弹出文件框并试听；「试听开始/结束音」有声。
  5. 外观：切换暗/亮色调；取色器与 Hex 输入改强调色，界面强调色实时变化；预览区按钮/进度条/选中态跟随；「恢复默认」还原。
- 遗留问题：无。
- 与原计划的偏差：新增了文件内局部 `ToggleRow`（仅 `SettingsStartupPanel` 内），未新增跨文件公共组件，符合“避免过度抽象”约束。
- 对后续任务的影响：4.1 需在真实运行时目视验证设置页各 tab 尺寸与折叠展开。
- 相关提交：见提交历史（升级：设置页与供应商配置迁移到令牌体系）。
