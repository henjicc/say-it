# 5.2 文档与技能同步

## 任务状态

- 状态：已完成
- 所属阶段：第五阶段-安装体验与文档收尾
- 前置任务：其余全部任务（以实际落地的接口为准写文档）
- 关联任务：无
- 是否可以并行：否（最后执行）

## 任务目标

把本次扩展固化为可复用的操作规范：`docs/rules/新增供应商与模型操作手册.md` 覆盖新场景（本地协议族、模型包、ocr 能力、v4 插件）；`.claude/skills/adapt-sayit-provider` 技能升级为可生成 v4 插件（含 ocr / translation 能力与 localNetwork 权限）；模型包制作流程有独立手册。

## 当前情况

- `docs/rules/新增供应商与模型操作手册.md`：覆盖场景一~四（同协议新模型/新协议族/新供应商/新能力类型），以真实操作记录为准，不含本地模型与模型包。
- `.claude/skills/adapt-sayit-provider/`：生成 v3 JavaScript 在线供应商插件的技能（调研 API → 实现 API v3 连接器 → 签名 → 产出 `.sayit`）；仓库有"同步技能文件"的提交惯例（75f3e9d）。
- `CLAUDE.md` 项目专属信息节提到插件相关约定的入口是该手册。
- 3.1 产出的模型包构建脚本此时已有实际用法（3.2/3.3/4.1 各打过包）。

## 实施方案与执行步骤

1. 手册扩展（按各任务执行记录中的真实操作写，不写设计推测）：
   - 场景五：新增本地协议族模型（模型包 manifest → `local-sherpa-*` 协议 → 涉及文件清单）。
   - 场景六：制作官方模型包（构建脚本用法、双形态、签名、发布位置）。
   - 场景四（能力类型）补充 ocr 的真实操作记录。
   - v3/v4 版本差异说明（哪些声明仅 v4 合法）。
2. `adapt-sayit-provider` 技能升级：manifest 模板升 v4；新增 ocr（`recognizeImage` 约定）与 translation 插件的生成指引；`localNetwork` 权限的使用条件与安全说明；保留生成 v3 兼容说明。
3. `CLAUDE.md` 项目专属信息节同步：数据目录机制、模型包入口、设置页三分类（只加必要的一两行指针，不展开）。
4. 检查 `docs/experience` 是否需要沉淀本次踩坑（如 sherpa-onnx 链接、迁移实现），按 CLAUDE.md 经验沉淀规则决定。
5. 全文交叉检查：手册引用的文件路径与函数名逐一与代码核对。

## 涉及内容

### 修改文件

- `docs/rules/新增供应商与模型操作手册.md`
- `.claude/skills/adapt-sayit-provider/`（技能文件）
- `CLAUDE.md`（项目专属信息节）

### 新增文件

- （按需）`docs/experience/` 下的经验文件。

## 验收标准

- [x] 手册覆盖已有 sherpa-onnx 引擎的模型包描述、受支持协议/参数、构建签名、双形态与安装验证；路径和函数名已逐项与代码核对。
- [x] 技能默认 v4 插件样例通过两份技能校验器、Node 烟雾测试与 Rust 宿主 `validate_plugin_dir` 契约测试。
- [x] 手册已移除“仅 v3 精确匹配”和用户可选“默认供应商”等过时说明，明确 v3/v4 边界与场景模型选择。
- [x] 任务总览已同步更新，整个任务标记完成。

## 执行记录

- 重写 `docs/rules/新增供应商与模型操作手册.md`：保留在线模型/协议/供应商/能力四类场景，并新增本地 sherpa/PP-OCR 模型包与官方模型包构建场景；命令、描述文件、协议、engine、必需参数、双形态产物和安装入口均来自现有实现。
- `.claude/skills/adapt-sayit-provider` 与 `.codex/skills/adapt-sayit-provider` 同步升级：默认模板改为 API v4；校验器兼容 v3/v4 并支持 `ocr`、`plugin-ocr-v1`、纯 OCR 无模型、`localNetwork`；接口文档补齐 `recognizeImage`、翻译和回环网络安全约束。
- 烟雾测试的临时执行目录改到系统临时目录，避免技能目录被监视或锁定时无法创建测试沙箱；同时禁止生成 `__pycache__`，不污染技能目录。
- `src-tauri/src/providers/plugin.rs` 新增技能模板宿主契约测试，直接读取 Codex 技能模板并通过 `validate_plugin_dir`，防止技能模板与宿主版本再次漂移。
- `CLAUDE.md` 增加统一数据根、`.sayit` 双形态、模型包入口和“供应商配置、场景选模型”指针；任务总览、实施方案与 2.3/4.2 交叉引用同步为最终产品决策。
- 经验沉淀评估：本次烟雾测试卡顿已通过技能脚本的确定性修复消除，不再额外创建重复经验文档。
- 最终验证：Rust 202 项测试中 199 通过、3 项真实模型环境测试忽略；`npx tsc --noEmit`、顺序执行的 `npm run ui:build` 通过；5.1 的最终 `npm run tauri:build` 已生成 NSIS 安装器。
