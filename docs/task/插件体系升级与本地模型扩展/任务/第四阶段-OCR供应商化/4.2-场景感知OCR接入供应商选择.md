# 4.2 场景感知 OCR 接入供应商选择

## 任务状态

- 状态：已完成（模型下拉、隐私确认与真实窗口捕获待用户人工回归）
- 所属阶段：第四阶段-OCR供应商化
- 前置任务：2.1（OcrProvider 工厂）、4.1（PP-OCR 供应商化）
- 关联任务：2.3（OCR 分区的默认供应商下拉即本任务的选择入口）
- 是否可以并行：否

## 任务目标

场景感知的 OCR 引擎选择从听写设置的 `OcrEngineKind` 枚举迁移到统一模型下拉框：已启用供应商的 OCR 模型全部进入 `activeAppContext` 场景选项，选择后按模型反查系统 OCR / PP-OCR 模型包 / 第三方插件 OCR；选用插件（云端）OCR 时有一次性隐私确认。旧设置值平滑迁移。供应商设置页不再提供“默认供应商”。

## 当前情况

- 引擎选择：`active_app_context/model.rs::OcrEngineKind { System, PpOcr }`，序列化字符串 `"system"` / `"ppocr"`；持久化在 `application/dictation.rs` 的 `active_app_context_ocr_engine`（约 99 行，默认值 System），消费点在捕获链路 `active_app_context/mod.rs::CaptureOptions::for_method(method, ocr_engine)` 与上下文调试窗口 `debug.rs`（支持临时覆盖引擎）。
- OCR 执行在专用 worker 线程（`ocr.rs` 的 `OCR_WORKER`），同步阻塞式管线，带 deadline 与取消轮询；插件 OCR 是异步网络调用，接入时要在 worker/调用侧做同步桥接（现有 `plugin_runtime` 的调用在 `capabilities.rs` 均为 async，场景感知链路本身由 `dictation.rs` 异步编排，执行时确认最小改动的桥接点并记录）。
- 2.1 已提供 `OcrProvider` 工厂（System / Plugin 变体），4.1 提供 PP-OCR 供应商；本任务把三者接到捕获链路。
- 场景感知截图约 ≤960px PNG，Base64 后体积远小于插件响应上限。

## 实施方案

1. **路由改造**：捕获链路按所选 OCR 模型反查供应商——系统 OCR → `windows_ocr`；PP-OCR → 已安装 `LocalModelSpec`；插件模型（或无显式模型的 OCR 插件虚拟项）→ `OcrProvider::Plugin.recognize(image, "activeAppContext")`。调试窗口同步改为模型覆盖。
2. **设置迁移**：新增 `activeAppContextOcrModel`；旧 `activeAppContextOcrEngine` 只保留一版读取兼容（`system` → `system-ocr`，`ppocr` → 已安装的 `local-ppocr-v6-tiny`，未安装则系统 OCR）。
3. **隐私确认**：在场景感知 OCR 模型下拉切到第三方插件时弹出 Modal；确认状态按供应商 id 持久化，后端再次强制校验，未确认时不会编码或发送截图。插件失败记录诊断并降级系统 OCR。
4. **超时**：插件 OCR 受场景感知现有 deadline 约束，超时按现有取消语义中止。

## 执行步骤

1. 捕获链路按供应商路由重构（保持系统/PP-OCR 行为逐字节等价，仅换选择机制）；`cargo test` 与上下文调试窗口回归两条内置路径。
2. 旧设置值迁移逻辑 + 测试（三种旧值 × 包安装与否）。
3. 隐私确认 UI 与持久化；插件 OCR 失败降级与日志。
4. 上下文调试窗口适配供应商覆盖。
5. 验证：`cargo test`、`npx tsc --noEmit`、`npm run ui:build`；调试窗口分别用系统 OCR / PP-OCR / 测试插件（可用假插件返回固定 blocks）各跑一次识别。
6. 用户实测：真实听写场景下场景感知润色正常。

## 涉及内容

### 修改文件

- `src-tauri/src/active_app_context/mod.rs`、`ocr.rs`、`debug.rs`、`model.rs`：路由与覆盖机制。
- `src-tauri/src/application/dictation.rs`：设置字段迁移与编排处改造。
- `ui/src/views/SettingsProviderPanel.tsx`（2.3 产物）：隐私确认弹窗触发。

### 可能废弃

- `OcrEngineKind` 与 `active_app_context_ocr_engine` 设置字段：迁移完成、兼容期结束后移除。

## 验收标准

- [x] 系统 OCR、真实 PP-OCR 夹具、插件 OCR 通用调用与场景路由测试通过；调试窗口已适配模型选择。
- [x] 旧版本设置按模型包安装状态等价迁移。
- [x] 未确认隐私时后端拒绝生成插件 OCR 调用，截图不会发送。
- [x] 插件 OCR 失败时记录诊断并降级系统 OCR，不阻塞听写流程。
- [x] `cargo test`、`npx tsc --noEmit`、`npm run ui:build` 通过。
- [x] 任务总览已同步更新。

## 执行记录

- “密钥与识别”的 ASR/OCR/翻译分区移除默认供应商控件；供应商只负责安装、配置和启用，运行场景始终从统一模型下拉选择。
- 模型目录新增 `activeAppContext` 场景：显式 OCR 模型直接列出；兼容现有无 `models` 的 v4 OCR 插件，为每个已启用 OCR 供应商生成一个下拉项。
- `activeAppContextOcrModel` 成为运行选择权威，后端从模型反查本地模型包或插件运行时；`OcrEngineKind` 仅保留旧设置迁移。
- 第三方 OCR 选择前使用 Modal 明示截图外发，授权按供应商 ID 持久化且后端强制校验；插件超时受场景感知 5 秒 deadline 约束，失败时降级 Windows 系统 OCR并写入诊断。
- 上下文调试窗口显示实际 OCR 模型，并明确第三方模型仍受隐私授权约束。真实窗口捕获与 Modal 鼠标交互按项目规则留给用户人工回归。
