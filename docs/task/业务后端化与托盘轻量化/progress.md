# 进度

## 2026-07-13 · 1.1 固化行为基线与应用契约

- 状态：已完成。
- 已完成：Rust 契约与只读快照骨架、前端镜像类型、进程树资源采样脚本、自动化验证、生命周期决策。
- 最终验收待办：用户交互回归、四类资源基线、主窗口/快捷键时延基线，统一见 `manual-test-checklist.md`。
- 阶段门禁：自动验证已通过；按用户决定，人工测试不阻塞后续阶段。

## 2026-07-13 · 1.2 统一配置持久化与状态快照

- 状态：已完成。
- 已完成：版本化后端配置、幂等旧数据导入、领域更新命令、完整快照、原子保存与备份恢复、前端启动桥和兼容镜像、自定义提示音文件迁移。
- 阶段门禁：Rust 全量测试、Cargo 检查、前端双入口构建及差异检查通过；人工升级与设置核对已移交 `manual-test-checklist.md`。

## 2026-07-13 · 2.1 模型与供应商目录改由后端输出

- 状态：已完成。
- 已完成：版本化 Rust 模型/供应商目录、场景选项和模型能力派生、供应商有效能力/配置描述/action、前端启动加载与薄查询。
- 阶段门禁：Rust 全量测试、Cargo 检查、前端双入口构建和差异检查通过；页面选项核对移交最终人工验收。

## 2026-07-13 · 2.2 补齐供应商能力接口与通用服务

- 状态：已完成。
- 已完成：文件识别、流式翻译、热词 customization 的统一能力 facade 与工厂；命令层去除供应商模块直连；通用热词命令及旧命令兼容代理；无网络 fake 覆盖标准事件、失败、取消和迟到结果。
- 阶段门禁：Rust 76 项测试、Cargo 检查、前端双入口构建和差异检查通过；真实服务验证移交 `manual-test-checklist.md`。

## 2026-07-13 · 3.1 迁移音频会话与听写运行时

- 状态：已完成。
- 已完成：Rust 内部事件分发、generation 音频租约、显式听写状态机、实时/文件识别、静音断流重连、延迟释放、规则处理、原生提示音、单次注入、指示器状态/波形摘要和前端命令/事件适配。
- 唯一权威：全局快捷键直接调用 Rust 服务；前端旧 `session/realtimeFlow/fileFlow/inject/indicatorBridge` 已删除，主 WebView 不再持有会话 ID、epoch、听写计时器或 PCM 缓冲。
- 阶段门禁：Rust 82 项全量测试、Cargo 检查、Vite 双入口构建、差异检查通过；真实快捷键、云端识别、音频与窗口交互已加入统一人工清单，不阻塞 3.2。

## 2026-07-13 · 3.2 迁移实时字幕翻译与直播输出编排

- 状态：已完成。
- 已完成：Rust 字幕状态机、麦克风/系统音频 owner、静音门控与重连、原文文档模型、分句翻译与 epoch/seq 乱序隔离、OBS 快照/路由/布局同步、后端指示器更新和前端隔离预览。
- 唯一权威：全局快捷键、ASR 会话、翻译请求、OBS 监控和真实字幕文本均由 Rust 持有；前端仅发送命令、投影 `domain-event` 并运行不触碰真实业务的预览模拟。
- 阶段门禁：Rust 87 项全量测试、Cargo 检查、Vite 双入口构建、旧前端运行时扫描和差异检查通过；真实设备、云端、OBS 与窗口交互已加入统一人工清单，不阻塞 3.3。
