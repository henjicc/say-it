# 最终人工测试清单

## 执行约定

- 用户决定：所有人工测试统一在全部任务完成后执行，不作为中间任务或阶段门禁。
- 后续任务发现新的鼠标、快捷键、真实音频、窗口、OBS 或性能验证项时，继续追加到本文件。
- 每项执行后记录：通过/失败、测试时间、版本/提交、模型与音源、错误信息或截图；失败须判断是迁移前既有问题还是本次回归。

## 功能回归

- [ ] 实时听写：说一句并停止，最终文本只注入一次；中途取消不注入且指示窗恢复。
- [ ] 文件听写：Fun-ASR-Flash、Qwen3-ASR-Flash 各执行一次且只注入一次；本地规则顺序和替换结果不变。
- [ ] 实时字幕：麦克风与系统音频分别持续滚动；有 OBS 环境时输出同步。
- [ ] 设置页静音测试：成功路径完成且页面状态恢复。
- [ ] 录音识别：真实音频完成转写；纯文本/字幕切换、复制、SRT 导出及播放器时间抽查正常。
- [ ] 文稿对齐：真实音频和逐行文稿执行；两种结果、缓存复用、SRT 文本与时间正常。
- [ ] 字幕编辑器：两个入口均验证 cue 整体/边缘拖动、吸附、缩放、播放头、文本编辑、±100ms 微调，输入框快捷键不误触发。

## 资源与时延

- [ ] 前台稳定 60 秒后执行：`powershell -File scripts/测量进程内存.ps1 -Condition foreground -OutputPath docs/task/业务后端化与托盘轻量化/baseline-foreground.json`。
- [ ] 关闭到托盘后执行：`powershell -File scripts/测量进程内存.ps1 -Condition tray-idle-60s -OutputPath docs/task/业务后端化与托盘轻量化/baseline-tray.json`。
- [ ] 完成一次听写后执行：`powershell -File scripts/测量进程内存.ps1 -Condition after-dictation -OutputPath docs/task/业务后端化与托盘轻量化/baseline-after-dictation.json`。
- [ ] 实时字幕运行中执行：`powershell -File scripts/测量进程内存.ps1 -Condition subtitles-running -OutputPath docs/task/业务后端化与托盘轻量化/baseline-subtitles.json`。
- [ ] 托盘点击到主窗口可交互测 5 次，记录每次毫秒数和中位数。
- [ ] 听写快捷键按下到指示器首次可见测 5 次，记录每次毫秒数和中位数。
- [ ] 最终托盘总工作集相对当前托盘基线下降至少 40%，空闲 CPU 不高于基线，恢复与快捷键响应增量不超过 100ms。
# 1.2 统一配置持久化与状态快照

- [ ] 使用包含四类历史偏好键的旧版本数据启动，确认设置、主题、字幕样式与本地规则无丢失。
- [ ] 重启应用确认设置由后端快照恢复；再次启动不应覆盖首次启动后新修改的值。
- [ ] 修改听写、字幕、模型对比和主题设置，回退旧版本后确认旧 localStorage 镜像仍可读取。
- [ ] 分别选择开始/结束自定义提示音，确认当前版本可以试听且重启后选择状态不丢失；3.1 后再核对原生播放。
- [ ] 准备损坏主配置且保留有效 `.bak`，确认从备份恢复；主备均损坏时应明确报错而非静默重置。
# 2.1 模型与供应商目录

- [ ] 听写、字幕、录音识别和模型对比下拉的选项、顺序、标签与迁移前一致。
- [ ] 默认实时模型为 `fun-asr-realtime-2026-02-28`，默认文件模型为 `fun-asr-flash-2026-06-15`。
- [ ] 设置页能显示供应商、API Key 状态并保存配置；热词入口仍正常显示和使用。

# 2.2 供应商通用能力

- [ ] 使用真实 API Key 分别完成 Fun-ASR 异步 OSS、Fun-ASR-Flash 同步 SSE、Qwen3-ASR-Flash 同步文件识别，核对上传/进度/完成事件、结果文本和取消行为与迁移前一致。
- [ ] 字幕开启 Qwen-MT 翻译，分别验证增量模型与非增量模型：译文逐步更新、最终完成一次、停止或重连后的迟到结果不污染新会话。
- [ ] 在供应商设置中依次保存、同步、清空热词；重启后本地显示与云端词表一致，旧词表 ID 失效时可自动重建。
- [ ] 暂时清空 API Key 后执行文件识别、翻译、保存/同步热词，均显示明确配置错误；无云端词表时清空热词仍可清理本地状态。

# 3.1 音频会话与听写运行时（实现完成后执行）

- [ ] 完全退出主窗口监听后，分别用切换模式和长按模式完成开始、停止、Esc 取消；最终文本只注入一次。
- [ ] 实时听写开启静音断流，先说话、静音超过阈值、再次说话，确认自动重连且两段文本均保留。
- [ ] 分别使用实时模型和文件模型验证成功、失败、快速取消、极短音频及停止后的迟到结果不污染下一会话。
- [ ] 逐项核对所有本地规则的顺序、开关、替换语义和非法规则提示；灾难性正则不得卡住应用。
- [ ] 内置与自定义开始/结束提示音均可播放，重启后仍有效，录音输入与播放输出无死锁。
- [ ] 主窗口隐藏、最小化和临时销毁三种情况下，指示窗状态、文本和波形摘要正常，主窗口不再收到完整 PCM。
- [ ] 长按模式快速点按（短于 260ms）不启动；超过阈值只启动一次，松开只停止一次；CapsLock/NumLock/ScrollLock 绑定时系统锁定状态不误切换。
- [ ] 听写结束后在麦克风保活时间内再次启动可复用设备；保活到期会释放；期间启动字幕/对比时不会被旧听写计时器误关闭设备。
- [ ] 保存包含反向引用、前后查找、命名捕获替换的自定义规则并核对结果；不支持的 flag/表达式保存时明确报错，灾难性回溯触发限制而非卡死。
- [ ] 自定义提示音文件损坏、丢失或输出设备不可用时显示明确错误，不静默改播内置音；设置页试听走系统原生输出。
- [ ] 完成文件听写后检查临时 WAV 不影响下一次会话；极短/空录音明确提示且不注入旧文本。
