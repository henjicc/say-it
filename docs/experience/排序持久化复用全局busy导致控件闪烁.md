# 排序持久化复用全局 busy 导致控件闪烁

## 现象

拖拽排序松手后，列表内的复选框、拖拽手柄等可禁用控件会同时短暂变淡再恢复，而文本内容没有变化。

## 原因

排序落位后的异步持久化复用了页面级 `busy` 状态。保存期间所有带 `disabled` 样式的控件都会切换透明度，因此快速保存表现为一次闪烁。

## 正确做法

将“阻止破坏性操作的可见 busy 状态”和“排序正在持久化的内部状态”分开。排序保存期间只暂停下一次排序，不切换复选框、手柄等控件的 `disabled` 外观；删除、恢复等真正需要阻塞界面的操作继续使用可见 busy 状态。
