# 插件适配技能工作区隔离

## 触发条件

使用供应商适配 Skill 生成插件时，宿主项目正好是当前工作目录，且用户只要求生成插件包。

## 正确做法

- 先要求用户提供项目外部的绝对插件工作目录；未提供时停止并询问，不能把当前目录当作默认输出位置。
- Skill 只能读取自身资源、外部供应商资料和外部工作目录；不得搜索、读取或写入宿主项目源码。
- 产物只能写到外部工作目录的 `dist/`，生成完成后交给用户手动通过插件管理安装；除非用户明确要求，不得自动安装。
- 初始化、打包和签名脚本都应接收并校验宿主项目禁止根目录，避免 Skill 指令被遗漏时仍把包写入项目。
